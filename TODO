platform:

- 监听文件变化（也可以timer轮询文件的status）

测试: sublog
测试: create log view should lock memory
业务接口
    range变化通过一个定时10ms detect range来通知上层
    FileLog监听文件变化
    ProcessLog监听其进程被关闭
错误处理（文件读取失败等）
日志高亮格式的中途变更（对根log的每一行清除日志格式，）